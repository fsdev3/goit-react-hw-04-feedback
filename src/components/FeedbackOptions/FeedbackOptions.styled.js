import { styled } from 'styled-components';

export const FeedbackButtons = styled.div`
  display: flex;
  flex-direction: row;
  gap: 8px;
  margin: 20px;

  button {
    padding: 4px 12px;
    font-size: 16px;
  }
`;
